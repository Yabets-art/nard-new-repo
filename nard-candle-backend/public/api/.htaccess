# Allow direct access to confirm-payment.php
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^confirm-payment$ confirm-payment.php [L]
</IfModule>

# Ensure PHP files are processed correctly
<FilesMatch "\.php$">
    SetHandler application/x-httpd-php
</FilesMatch> 